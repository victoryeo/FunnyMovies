<div class="container">
  <div class="row">
    <div class="col-md-6 col-sm-12">
      <h2>Share new video</h2>
      <%= simple_form_for @video, html: { multipart: true } do |f| %>
        <%= f.input :name %>
        <%= f.input :video_url %>
        <%= f.button :submit, class: 'btn-primary', value: 'Share Video' %>
      <% end %>
    </div>
  </div>
</div>
